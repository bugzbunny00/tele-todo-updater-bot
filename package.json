{
  "name": "telegram-task-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:remote": "wrangler d1 execute task_bot_db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute task_bot_db --local --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
